# Python Lists (Beginner Friendly)

Lists in Python are ordered, mutable collections of items.

Example:

numbers = [1, 2, 3]

You can:
- Append items with numbers.append(4)
- Iterate with for number in numbers:
- Use list comprehensions like [x * 2 for x in numbers]
